names = ["David", "John", "Annabelle", "Johnathan", "Veronica"]

def  cont(k):
    if len(k)>5:
        return True
    else:
        return False

mylist=list(filter(cont,names))
print(mylist)

'''
result=list(filter(lambda x:len(x)>5,names))

print(result)
'''
